const REQUEST_RATE = 10;
const API_URL = "https://api.github.com";
const ACCESS_TOKEN = process.env.GITHUB_ACCESS_TOKEN;

const LABELS = ["bug", "good first issues", "help wanted"];

export { REQUEST_RATE, API_URL, ACCESS_TOKEN, LABELS };
