import { FormatIssue } from "./../types/issues";

const filterIssues = (formatIssue: FormatIssue[], labels: string[]) => {
  console.log("filteredIssues", formatIssue[0]);
};

export default filterIssues;
